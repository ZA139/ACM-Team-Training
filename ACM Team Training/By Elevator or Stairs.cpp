#include<cstdio>
const int maxn = 2 * 10e5;
int a[maxn], b[maxn],ans[maxn];
int main(void) {
	int t, s;
	scanf("%d%d", &t, &s);
	for (int i = 0; i < t; i++)
	{
		scanf("%d", &a[i]);
	}
	for (int i = 0; i < t; i++)
	{
		scanf("%d", &b[i]);
	}
	
	return 0;
}