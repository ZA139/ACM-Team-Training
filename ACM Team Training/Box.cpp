#include<cstdio>
#include<cstring>
#include<iostream>
using namespace std;
typedef struct ass {
	int x;
	int y;
};
ass p[6];
int main(void) {
	while (cin>>p[0].x>>p[0].y >> p[0].x >> p[0].y >> p[0].x >> p[0].y >> p[0].x >> p[0].y >> p[0].x >> p[0].y >> p[0].x >> p[0].y >> p[0].x >> p[0].y) {

	}
}